# Far-side pile glyph reference

One manually reviewed digit `5`, from `sessions/20260831-141529/frame-007691-142819452.jpg`, normalized crop `(0.731,0.086,0.746,0.114)`.
Reproduce using `--recording-pile-glyph`. This is training, not held-out evidence.

Used only with the far-side pile component-size profile. Existing similarity and runner-up margin thresholds are unchanged. `frame-007743-142824638.jpg` is a later evaluation frame, not an independent recording. Existing other-recording HUD/leader/title tests remain negative/regression coverage.
